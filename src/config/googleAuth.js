// Firebase handles the full Google OAuth flow via signInWithPopup.
// No client IDs or redirect URIs are needed here — all OAuth config
// lives in the Firebase project (firebaseConfig in firebase.js).
